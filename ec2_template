Resources:
  UserDataInstance:
    Type: "AWS::EC2::Instance"
    Properties:
      ImageId: 'ami-062f7200baf2fa504'
      KeyName: 'ec2BenKeyPair'
      UserData: 
        Fn::Base64: 
          !Sub |
          #!/bin/bash
          yum -y update;
          yum -y upgrade;
          yum -y install git;
          yum -y install awslogsd;
          systemctl start awslogsd;
          cd ;
          curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash;
          ~/.nvm/nvm.sh nvm install 10.18;
          git clone https://github.com/xDOASx/aws_s3_ec2.git;
          cd aws_s3_ec2;
          npm -y i;
          npm update -y;